# pkg-config Metadata for clanDisplay

prefix=@prefix@
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
includedir=${prefix}/include/ClanLib-@LT_RELEASE@

Name: clanDisplay
Description: Display support for ClanLib
Version: @VERSION@
Requires: clanCore-@LT_RELEASE@ = @VERSION@
Libs:   -L${libdir} -lclan@CLANLIB_RELEASE@Display @extra_LIBS_clanDisplay@ @clanDisplay_LIBS@
Cflags: -I${includedir} @extra_CFLAGS_clanDisplay@

# EOF #
